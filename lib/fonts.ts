import { Gurajada, UnifrakturCook } from "next/font/google";

export const unifrakturCook = UnifrakturCook({
  weight: ["700"],
  subsets: ["latin"],
});

export const gurajada = Gurajada({
  weight: ["400"],
  subsets: ["latin"],
});
